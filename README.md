# How to run this project:
1. Open your terminal and run yarn
2. Wait a while for all packages to install
3. Once packages are installed, use 'yarn start' to run the project
4. The project should automatically open in localhost:3000 so kindly ensure you have nothing else open there